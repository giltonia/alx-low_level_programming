#include <stdio>
/**
 * main - A programme that print a line using the printf function
 * Return: (0) (Succes)
 */
int main(void)
{
printf("with proper grammer, but the outcome is a piece of art,\n")
return (0);
}
